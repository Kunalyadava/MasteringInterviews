(name=>`Hello from ${name}!`)('vivek');

// the function is called with the argument 'vivek', so the name parameter in the function body
//  becomes 'vivek'. The template literal Hello from ${name}! then evaluates to Hello from vivek!.